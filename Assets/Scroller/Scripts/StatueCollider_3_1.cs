using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class StatueCollider_3_1 : StatueCollider
{
    // Start is called before the first frame update
    void Start()
    {
        //Debug.Log("This works");
    }

    // Update is called once per frame
    void Update()
    {
           
    }
}
